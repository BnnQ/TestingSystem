namespace TestingSystem.Constants.Authorization
{
    public static class NavigationKeys
    {
        public const string Authentication = nameof(Authentication);
        public const string Registration = nameof(Registration);
    }
}
<Window x:Class="TestingSystem.Views.Teacher.CategoryEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:ag="clr-namespace:WpfAutoGrid;assembly=WpfAutoGrid.Core"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="{StaticResource ResourceKey=MainFontSize}"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Background="{DynamicResource MaterialDesignPaper}"
        Title="Редактор категории" Height="350" Width="{StaticResource ResourceKey=MainEditViewWidth}"
        MaxHeight="350" MaxWidth="{StaticResource ResourceKey=MainEditViewWidth}">
    <ag:AutoGrid Rows="1*, 1*" Columns="1*">
        <Grid.Resources>
            <Style TargetType="TextBox" BasedOn="{StaticResource OutlinedTextBoxStyle}">
                <Setter Property="Margin" Value="{StaticResource ResourceKey=MarginOfEditControl}"/>
            </Style>
        </Grid.Resources>
        
        <TextBox md:HintAssist.Hint="Название" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <ag:AutoGrid Rows="1*" Columns="1*, 1*">
            <Button Style="{StaticResource ResourceKey=ConfirmButtonStyle}" Command="{Binding ConfirmAsyncCommand}">
                <TextBlock Text="{StaticResource ConfirmButtonText}"/>
            </Button>
            <Button Style="{StaticResource ResourceKey=CancelButtonStyle}" Command="{Binding CancelCommand}">
                <TextBlock Text="{StaticResource CancelButtonText}"/>
            </Button>
        </ag:AutoGrid>
    </ag:AutoGrid>
</Window>
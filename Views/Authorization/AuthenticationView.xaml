<UserControl x:Class="TestingSystem.Views.Authorization.AuthenticationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:ag="clr-namespace:WpfAutoGrid;assembly=WpfAutoGrid.Core"
             xmlns:converters="clr-namespace:TestingSystem.Views.Converters"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="17"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}"
             Background="{DynamicResource MaterialDesignPaper}"
             d:DesignHeight="600" d:DesignWidth="450">
    <ag:AutoGrid Rows="1*, 9*" Columns="1*">
        <Grid.Resources>
            <converters:BoolInverserConverter x:Key="boolInverserConverter"/>

            <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignTextBlock}">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="FontFamily" Value="{DynamicResource MaterialDesignFont}"/>
                <Setter Property="FontSize" Value="17"/>
                <Setter Property="Margin" Value="5"/>
            </Style>

            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="FontSize" Value="17"/>
                <Setter Property="Margin" Value="5"/>
            </Style>

            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatMidBgButton}">
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="50"/>
                <Setter Property="md:ShadowAssist.ShadowDepth" Value="Depth0"/>
                <Setter Property="md:ButtonAssist.CornerRadius" Value="10"/>
            </Style>
        </Grid.Resources>

        <Button Grid.ColumnSpan="2" Style="{StaticResource MaterialDesignFlatButton}"
                Command="{Binding SwitchToRegistrationNavigationCommand}">
            <TextBlock Text="Зарегистрироваться"/>
        </Button>

        <ag:AutoGrid Rows="1*, 1*, 1*" Columns="1*" IsEnabled="{Binding AuthenticationBackgroundWorker.IsBusy, Converter={StaticResource boolInverserConverter}}">
            <TextBox Text="{Binding Username}" Width="300" md:HintAssist.Hint="Имя пользователя" BorderThickness="2" 
                     BorderBrush="{DynamicResource MaterialDesignDivider}"/>

            <TextBox Text="{Binding Password}" Width="300" md:HintAssist.Hint="Пароль" BorderThickness="2" 
                     BorderBrush="{DynamicResource MaterialDesignDivider}"/>

            <Button Command="{Binding AuthenticateAsyncCommand}" Height="53" Width="300">
                <TextBlock Text="Войти"/>
            </Button>
        </ag:AutoGrid>
        
    </ag:AutoGrid>
</UserControl>
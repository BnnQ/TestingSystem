<UserControl x:Class="TestingSystem.Views.Authorization.AuthenticationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:ag="clr-namespace:WpfAutoGrid;assembly=WpfAutoGrid.Core"
             xmlns:converters="clr-namespace:TestingSystem.Views.Converters"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="{StaticResource ResourceKey=MainFontSize}"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             FontFamily="{DynamicResource MaterialDesignFont}"
             Background="{DynamicResource MaterialDesignPaper}"
             d:DesignHeight="600" d:DesignWidth="450">
    <ag:AutoGrid Rows="1*, 9*" Columns="1*">
        <Grid.Resources>
            <converters:BoolInverserConverter x:Key="boolInverserConverter"/>

            <Style TargetType="TextBox" BasedOn="{StaticResource ResourceKey=OutlinedTextBoxStyle}">
                <Setter Property="Width" Value="300"/>
            </Style>
            <Style TargetType="Button" BasedOn="{StaticResource ResourceKey=RoundedRectangularButtonStyle}"/>
            <Style TargetType="TextBlock" BasedOn="{StaticResource ResourceKey=MainTextBlockStyle}"/>
        </Grid.Resources>

        <Button Grid.ColumnSpan="2" Style="{StaticResource ResourceKey=FlatButtonWithoutHighlightStyle}"
                Command="{Binding SwitchToRegistrationNavigationCommand}" Padding="-5">
            <TextBlock Text="Зарегистрироваться"/>
        </Button>

        <ag:AutoGrid Rows="1*, 1*, 1*" Columns="1*" IsEnabled="{Binding AuthenticationBackgroundWorker.IsBusy, Converter={StaticResource boolInverserConverter}}">
            <TextBox Text="{Binding Username}" md:HintAssist.Hint="Имя пользователя"/>
            <TextBox Text="{Binding Password}" md:HintAssist.Hint="Пароль"/>

            <Button Command="{Binding AuthenticateAsyncCommand}" Height="53" Width="300">
                <TextBlock Text="Войти"/>
            </Button>
        </ag:AutoGrid>
        
    </ag:AutoGrid>
</UserControl>